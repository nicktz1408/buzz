package GameLogic;

/**
 * Enum to indicate the status of the fetchNextQuestion function on the GameFacade
 */
public enum FetchNextQuestionStatus {
    NEXT_QUESTION,
    NEXT_ROUND,
    GAME_FINISHED
}
